export const ZOD_STRINGS = {
  OBJECT: 'z.object({',
  ARRAY: 'z.array(',
  ENUM: 'z.enum(',
  STRING: 'z.string()',
  NUMBER: 'z.number()',
  BOOLEAN: 'z.boolean()',
  UNION: 'z.union(',
  NULL: 'z.null()',
  OPTIONAL: '.optional()',
  REGEX: '.regex(',
  MIN: '.min(',
  MAX: '.max(',
  GT: '.gt(',
  LT: '.lt(',
  INT: '.int()',
  UUID: '.uuid()',
  EMAIL: '.email()',
  URL: '.url()',
  DATE: '.date()',
  DATETIME: '.datetime()',
}

export const ZOD_REGEX = {
  PHONE: '^\\+?[1-9]\\d{1,14}$',
}
