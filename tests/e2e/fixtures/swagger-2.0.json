{
  "swagger": "2.0",
  "info": {
    "title": "Swagger 2.0 builders fixture",
    "version": "1.0.0"
  },
  "basePath": "/api",
  "produces": ["application/json"],
  "paths": {
    "/legacy-pets/{id}": {
      "get": {
        "operationId": "getLegacyPet",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "type": "integer",
            "format": "int64"
          }
        ],
        "responses": {
          "200": {
            "description": "A legacy pet",
            "schema": {
              "$ref": "#/definitions/LegacyPet"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "LegacyPet": {
      "type": "object",
      "required": ["id", "name", "aliases"],
      "properties": {
        "id": {
          "type": "integer",
          "format": "int64",
          "minimum": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    }
  }
}
